<%= form_with(model: @comment, url: user_post_comments_path, method: :post) do |form| %>
  <div>
    <%= form.label :comment %>
    <%= form.text_area :text %>
  </div>
  <%= form.submit "Add comment" %>
<% end %>
